trigger changeEmail on Contact (after insert) {
	/*String newEmail = 'hello@world.com';
    List<Contact> listContact = [SELECT Id, LastName, Email FROM Contact WHERE Id IN :trigger.new];
    for(Contact contact : listContact){
        contact.Email = newEmail;
    }
    update listContact;*/
}